import 'package:flutter/material.dart';

class VideoSectionScreen extends StatelessWidget {
  const VideoSectionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
